printNote=function(orderId){
  const invoiceLang=document.querySelector('#adminLanguage')?.value||'ru',tx={ru:{note:'Накладная',restaurant:'Ресторан',bake:'Выпечка',delivery:'Доставка',product:'Товар',quantity:'Количество',price:'Цена',amount:'Сумма',plain:'Обычный хлеб',pumpkin:'Хлеб из тыквы',pcs:'шт.',net:'Без НДС',vat:'НДС',total:'Итого',previous:'Задолженность до поставки',paid:'Оплачено при отгрузке',debt:'Общая задолженность',terms:'Условия оплаты'},en:{note:'Delivery note',restaurant:'Restaurant',bake:'Bake',delivery:'Delivery',product:'Product',quantity:'Quantity',price:'Price',amount:'Amount',plain:'Plain bread',pumpkin:'Pumpkin bread',pcs:'pcs',net:'Excl. VAT',vat:'VAT',total:'Total',previous:'Balance before shipment',paid:'Paid on shipment',debt:'Total balance due',terms:'Payment terms'},es:{note:'Albarán',restaurant:'Restaurante',bake:'Horneado',delivery:'Entrega',product:'Producto',quantity:'Cantidad',price:'Precio',amount:'Importe',plain:'Pan clásico',pumpkin:'Pan de calabaza',pcs:'uds.',net:'Sin IVA',vat:'IVA',total:'Total',previous:'Deuda antes de la entrega',paid:'Pagado en la entrega',debt:'Deuda total',terms:'Condiciones de pago'}}[invoiceLang];
  const invoiceProductName=id=>typeof productRegistry!=='undefined'?(productRegistry.find(p=>p.id===id)?.names?.[invoiceLang]||productRegistry.find(p=>p.id===id)?.names?.ru||id):(id==='plain'?tx.plain:tx.pumpkin);
  const n=deliveryNotes.find(x=>x.orderId===orderId),o=orders.find(x=>x.id===orderId);
  if(!n)return;
  const r=restaurant(n.restaurantId),b={...invoiceDefaults,...bakerySettings,...(n.bakery||{})};
  const prefix=b.notePrefix||'DN-',number=`${prefix}${String(n.number).padStart(4,'0')}`;
  const taxOn=n.taxEnabled!==undefined?Boolean(n.taxEnabled):Number(n.taxRate||0)>0;
  const subtotal=Number(n.subtotal??n.total),total=taxOn?Number(n.total):subtotal;
  const paid=Number(n.paid||0),balanceAfter=Number(n.balanceAfter||0),previousDebt=balanceAfter-total+paid;
  const totals=taxOn
    ? `${tx.net}: ${euro(subtotal)}<br>${b.showTax?`${tx.vat} ${n.taxRate||0}%: ${euro(n.tax||0)}<br>`:''}${tx.total}: <strong>${euro(total)}</strong>`
    : `${tx.total}: <strong>${euro(total)}</strong>`;
  const w=window.open('','_blank');if(!w)return;
  w.document.write(`<title>${number}</title><style>body{font:15px Arial;max-width:800px;margin:40px auto;color:#17231b}h1{font:38px Georgia;margin-bottom:5px}.meta{display:grid;grid-template-columns:1fr 1fr;gap:30px}.doc{text-align:right}table{width:100%;border-collapse:collapse;margin-top:28px}td,th{padding:10px;border-bottom:1px solid #ccc;text-align:left}.total{text-align:right;font-size:17px;line-height:1.65}.debt-total{font-size:20px;border-top:1px solid #aaa;padding-top:6px}.terms,.footer{margin-top:24px;padding:12px;background:#f4f4ef}.sign{margin-top:70px;display:flex;justify-content:space-between}</style><h1>Panora</h1><div class="meta"><p><strong>${b.legalName||'Panora'}</strong><br>${b.taxId||''}<br>${b.address||''}<br>${b.email||''}<br>${b.phone||''}</p><div class="doc"><h2>${b.noteTitle||tx.note} ${number}</h2><p>${n.date}</p></div></div><p>${tx.restaurant}: <strong>${r.name}</strong><br>${r.address||'—'}<br>${tx.bake}: ${o?.date||n.date}<br>${tx.delivery}: ${o?.deliveryDate||o?.date||n.date}</p><table><tr><th>${tx.product}</th><th>${tx.quantity}</th><th>${tx.price}</th><th>${tx.amount}</th></tr>${n.items.map(i=>`<tr><td>${invoiceProductName(i.product)}</td><td>${i.quantity} ${tx.pcs}</td><td>${euro(n.prices[i.product])}</td><td>${euro(i.quantity*n.prices[i.product])}</td></tr>`).join('')}</table><p class="total">${totals}<br>${tx.previous}: ${euro(previousDebt)}<br>${tx.paid}: ${euro(paid)}<br><strong class="debt-total">${tx.debt}: ${euro(balanceAfter)}</strong></p>${b.paymentTerms?`<p class="terms"><strong>${tx.terms}:</strong> ${b.paymentTerms}</p>`:''}${b.noteFooter?`<p class="footer">${b.noteFooter}</p>`:''}<div class="sign"><span>${b.bakerySignature||'Panora'} __________________</span><span>${b.customerSignature||tx.restaurant} __________________</span></div>`);
  w.document.close();w.print();
};
